import type { Metadata } from 'next'
import ToolClient from './ToolClient'

export const metadata: Metadata = {
  title: 'Barkod, QR Kod & ZPL Viewer — DevOneKit',
  description: 'QR kod ve barkod üretin, ZPL etiketlerinizi önizleyin. EAN-13, Code128, UPC-A, QR kod ve Zebra ZPL etiket görüntüleyici. Ücretsiz, kayıtsız.',
  keywords: ['zpl viewer', 'zpl önizleme', 'zpl to png', 'zebra zpl online', 'zpl label preview', 'barkod üretici', 'qr kod üretici', 'ean-13 generator', 'zpl editor online'],
}

export default ToolClient
