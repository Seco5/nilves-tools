import type { Metadata } from 'next'
import ToolClient from './ToolClient'

export const metadata: Metadata = {
  title: 'Difr Checker — Compare Text Online — DevOneKit',
  description: 'Compare two text blocks side by side with a live diff viewer. Highlights additions, deletions and unchanged lines.',
  keywords: ['difr checker', 'text compare', 'diff tool', 'code diff'],
}

export default ToolClient
