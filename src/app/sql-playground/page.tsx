import type { Metadata } from 'next'
import ToolClient from './ToolClient'

export const metadata: Metadata = {
  title: 'SQL Playground — Learn SQL Online — DevOneKit',
  description: 'Interactive SQL playground with a pre-loaded dataset. Learn SELECT, WHERE, JOIN, GROUP BY, subqueries and window functions with live results.',
  keywords: ['sql playground', 'learn sql', 'sql tutorial', 'sql online', 'sql editor'],
}

export default ToolClient
