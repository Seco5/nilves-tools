import type { Metadata } from 'next'
import ToolClient from './ToolClient'

export const metadata: Metadata = {
  title: 'SQL Formatter & Validator — Nilves Dev Tools',
  description: 'Free online SQL formatter and validator. Supports MySQL, PostgreSQL, SQLite, T-SQL, Oracle. Format, minify and syntax highlight your SQL queries instantly.',
  keywords: ['sql formatter', 'sql beautifier', 'sql validator', 'mysql formatter', 'postgresql formatter', 'sql minifier', 'format sql online', 'sql syntax highlight'],
}

export default ToolClient
