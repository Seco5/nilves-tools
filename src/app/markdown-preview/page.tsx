import type { Metadata } from 'next'
import ToolClient from './ToolClient'

export const metadata: Metadata = {
  title: 'Markdown Preview — DevOneKit',
  description: 'Write Markdown in the left pane and see a live rendered preview on the right. Supports headings, bold, code blocks and more.',
  keywords: ['markdown preview', 'markdown editor', 'markdown renderer', 'markdown live'],
}

export default ToolClient
