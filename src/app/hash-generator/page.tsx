import type { Metadata } from 'next'
import ToolClient from './ToolClient'

export const metadata: Metadata = {
  title: 'Hash Generator MD5 SHA256 — DevOneKit',
  description: 'Generate MD5, SHA-1, SHA-256 and SHA-512 hashes from any text. All hashing runs locally in your browser.',
  keywords: ['hash generator', 'md5', 'sha256', 'sha-1', 'sha-512', 'checksum'],
}

export default ToolClient
