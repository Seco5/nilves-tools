import type { Metadata } from 'next'
import ToolClient from './ToolClient'

export const metadata: Metadata = {
  title: 'Number Base Converter — DevOneKit',
  description: 'Convert numbers between decimal, binary, octal and hexadecimal bases instantly.',
  keywords: ['number base converter', 'binary converter', 'hex converter', 'decimal to binary', 'base conversion'],
}

export default ToolClient
