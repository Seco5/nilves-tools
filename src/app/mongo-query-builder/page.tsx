import type { Metadata } from 'next'
import ToolClient from './ToolClient'

export const metadata: Metadata = {
  title: 'MongoDB Sorgu Oluşturucu — Filter & Aggregate Pipeline | DevOneKit',
  description: 'MongoDB find ve aggregate sorgularını görsel olarak oluşturun. Shell, Node.js ve Python kod çıktısı alın. Ücretsiz, kayıtsız, tarayıcıda çalışır.',
  keywords: ['mongodb query builder', 'mongo sorgu oluşturucu', 'aggregate pipeline builder', 'mongodb aggregate generator', 'mongo query generator', 'mongodb filter builder', 'nosql query builder', 'pymongo query generator'],
}

export default ToolClient
