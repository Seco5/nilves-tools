import type { Metadata } from 'next'
import ToolClient from './ToolClient'

export const metadata: Metadata = {
  title: 'Cron Expression Explainer — DevOneKit',
  description: 'Parse and explain cron expressions in plain English. Includes common examples and per-field breakdowns.',
  keywords: ['cron expression', 'cron parser', 'cron explainer', 'crontab', 'cron schedule'],
}

export default ToolClient
