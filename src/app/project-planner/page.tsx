import type { Metadata } from 'next'
import ToolClient from './ToolClient'

export const metadata: Metadata = {
  title: 'Project Planner — Build Your Roadmap — Nilves Dev Tools',
  description: 'Free browser-based project planner. Add tasks, set dependencies, generate Gantt charts and export to Excel. No signup needed.',
  keywords: ['project planner', 'gantt chart', 'roadmap generator', 'task planner', 'project timeline', 'ms project alternative', 'free project management'],
}

export default ToolClient
