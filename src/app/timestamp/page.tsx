import type { Metadata } from 'next'
import ToolClient from './ToolClient'

export const metadata: Metadata = {
  title: 'Unix Timestamp Converter — Nilves Dev Tools',
  description: 'Convert Unix timestamps to human-readable dates and vice versa. Supports seconds and milliseconds.',
  keywords: ['unix timestamp', 'timestamp converter', 'epoch time', 'unix time', 'date converter'],
}

export default ToolClient
