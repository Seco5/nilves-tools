import type { Metadata } from 'next'
import XmlFormatterClient from './XmlFormatterClient'

export const metadata: Metadata = {
  title: 'XML Formatter & Validator — DevOneKit',
  description: 'Format, validate and minify XML online. Free, browser-based, no signup required.',
  keywords: ['xml formatter', 'xml validator', 'xml beautifier', 'xml minifier', 'pretty print xml'],
}

export default XmlFormatterClient
