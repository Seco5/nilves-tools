import type { Metadata } from 'next'
import ToolClient from './ToolClient'

export const metadata: Metadata = {
  title: 'CSV to JSON Converter — Nilves Dev Tools',
  description: 'Convert CSV to JSON or JSON arrays back to CSV instantly in your browser. No upload, no server.',
  keywords: ['csv to json', 'json to csv', 'csv converter', 'csv parser'],
}

export default ToolClient
